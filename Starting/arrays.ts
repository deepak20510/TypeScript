function getMax(nums:number[]){}

getMax([1.2,3])
interface Address{
    city:string;
    pincode:string;
}

interface User{
    name:string;
    age:number;
    address:Address[];
}
let user:User={
    name:"Deepak",
    age:22,
    address:[{city:"Jamshedpur",pincode:"812100"}]
}